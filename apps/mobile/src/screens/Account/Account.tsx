import {Screen, Layout, Typography} from 'ui'

const Account = () => {
  return (
    <Screen>
      <Layout padding="extraLarge" flex={1}>
        <Typography variant="largeTitle">Account</Typography>
      </Layout>
    </Screen>
  )
}

export default Account
