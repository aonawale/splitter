import {Screen, Layout, Typography} from 'ui'

const Groups = () => {
  return (
    <Screen>
      <Layout padding="extraLarge" flex={1}>
        <Typography variant="largeTitle">Groups</Typography>
      </Layout>
    </Screen>
  )
}

export default Groups
