import {Screen, Layout, Typography} from 'ui'

const Activities = () => {
  return (
    <Screen>
      <Layout padding="extraLarge" flex={1}>
        <Typography variant="largeTitle">Activities</Typography>
      </Layout>
    </Screen>
  )
}

export default Activities
